function multiplyAll(arr) {
  let product = 1;
  // Only change code below this line
  for (let o = 0; o < arr.length; o++) {
    for (let p = 0; p < arr[o].length; p++) {
      product = product * arr[o][p];
    }
  }
  // Only change code above this line
   return product;
}

multiplyAll([[1, 2], [3, 4], [5, 6, 7]]);
